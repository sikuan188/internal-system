# PCMS SSL Configuration for Docker Compose
# 設置 SSL 相關環境變量

# SSL Certificate paths (相對於 docker-compose.yml)
SSL_CERT_PATH=./ssl/self-signed/server.crt
SSL_KEY_PATH=./ssl/self-signed/server.key

# SSL Configuration
SSL_ENABLED=true
FORCE_HTTPS=false

# Domain configuration
DOMAIN_NAME=pcms-local.test
